from tkinter import *

menu_inicial = Tk()
menu_inicial.title("Sistema gerenciador de clientes")
menu_inicial.geometry("700x700")
menu_inicial.resizable(True, True)
menu_inicial.minsize(700, 700)
menu_inicial.maxsize(700, 700)

menu_inicial.mainloop()